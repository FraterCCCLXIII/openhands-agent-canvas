import { CodeReviewHooksPanel } from "#/components/features/code-review/code-review-hooks-panel";

/**
 * Temporary home for Code Review hooks (#1691) until repo-level governance
 * returns to the Code Review surface.
 */
function HooksSettingsScreen() {
  return <CodeReviewHooksPanel />;
}

export default HooksSettingsScreen;
